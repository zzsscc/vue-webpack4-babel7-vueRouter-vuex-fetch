/**
 * 包含所有的 mutations 的常量 
 */

// 增加count
export const ADDCOUNT_MUTATION = 'addCount'

// 往data.c后拼接数据
export const PUSHDATA_MUTATION = 'pushData'

// 改变配置
export const CHANGECONFIG_MUTATION = 'changeConfig'
